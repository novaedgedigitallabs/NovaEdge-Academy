version: '3.8'

services:
  backend:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart: unless-stopped
    # If your backend requires a local MongoDB, you can uncomment this:
    # depends_on:
    #   - mongodb

#  mongodb:
#    image: mongo:latest
#    ports:
#      - "27017:27017"
#    volumes:
#      - mongo-data:/data/db

#volumes:
#  mongo-data:
